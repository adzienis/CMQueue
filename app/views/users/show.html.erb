
<%= render partial: "shared/back_navbar" %>


<h2>
  <b>
    <%= User.name.humanize %>
  </b>
</h2>

<%= render partial: "shared/show_model",
           locals: { model_instance: @user, options: { except: [:uid, :provider] }}
%>