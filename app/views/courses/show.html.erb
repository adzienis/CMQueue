<h1> Course </h1>

<%= render partial: "shared/show_model",
           locals: {
             model_instance: @course,
             options: {
               except: [:course_code, :student_code, :id, :course_id, :access_grants, :access_tokens, :users],
               actions: [:edit]
             }
           } %>