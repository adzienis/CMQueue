<h1> Questions </h1>

<%= render partial: "shared/search_form",
           locals: {
             records: @records,
             ransack: @questions_ransack,
             course: @course,
             pagy: @pagy,
             options: {
               extra_columns: [{ user: :given_name},{question_state: :state}],
               except: [:title, :notes, :user_id, :course_id, :id]
             }
           } %>