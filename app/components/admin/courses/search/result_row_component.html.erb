<tr>
  <td>
    <h5 class="fw-bold">
      <%= link_to course.name, course_path(course) %>
    </h5>
    <hr>
    <span class="badge <%= open_class %>"> <%= open_msg %></span>
  </td>
  <td>
    <%= course.created_at.strftime("%a, %x, %I:%M %p") %>
  </td>
</tr>