
<h1>
  Roles
</h1>


<%= render partial: "shared/search_form",
           locals: {
             records: @records,
             ransack: @roles_ransack,
             course: @course,
             pagy: @pagy,
             options: {
               except: [:course_id, :id]
             }
           } %>

