<turbo-stream action="replace" target="<%= dom_id(@question) %>">
  <template>
    <%= render(Courses::Feed::QuestionCardComponent.new(question: @question, current_user: current_user)) %>
  </template>
</turbo-stream>
